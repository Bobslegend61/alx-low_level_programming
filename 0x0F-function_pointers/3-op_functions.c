/**
 * op_add - Add two numbers
 * @a: First number
 * @b: secont number
 *
 * Return: The result
 */
int op_add(int a, int b)
{
        return (a + b);
}

/**
 * op_add - Operate two numbers
 * @a: First number
 * @b: secont number
 *
 * Return: The result
 */
int op_sub(int a, int b)
{
        return (a - b);
}

/**
 * op_add - Operate two numbers
 * @a: First number
 * @b: secont number
 *
 * Return: The result
 */
int op_mul(int a, int b)
{
        return (a * b);
}

/**
 * op_add - Operate two numbers
 * @a: First number
 * @b: secont number
 *
 * Return: The result
 */
int op_div(int a, int b)
{
        return (a / b);
}

/**
 * op_add - Operate two numbers
 * @a: First number
 * @b: secont number
 *
 * Return: The result
 */
int op_mod(int a, int b)
{
        return (a % b);
}

